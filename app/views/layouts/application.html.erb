<!DOCTYPE html>
<html lang="ja">
<html>
  <head>
    <title>Kotonoha</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="ここにサイト説明を入れます">
    <%# <link rel="stylesheet" href="css/style.css"> %>
    <%= csrf_meta_tags %> 
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
  </head>
  

  <body>
    <% unless controller_name == 'sessions' && action_name == 'new' || (controller_path == 'devise/registrations' && action_name == 'new')  %>
     <header>
      <div class="header__bar row">
        <h1 class="grid-6"><a href="/">言の葉滔々</a></h1>
         <% if user_signed_in? %>
          <div class="right_contents">
            <div class="user_nav grid-6">
              <%= link_to current_user.nickname, "/users/#{current_user.id}" %>
            </div>
            <div class="posting">
              <%= link_to "投稿する", new_kotoba_path, class: "post" %>
            </div>
            <div class="log_out">
               <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete } %>
            </div>
          </div>
         <% else %>
          <div class="grid-6">
            <%= link_to "ログイン", new_user_session_path, class: "post" %>
            <%= link_to "新規登録", new_user_registration_path, class: "post" %>
          </div>
         <% end %>
      </div>
     </header>
    <% end %>

    <%= yield %>
    <footer>
      <footer>
        <small>Copyright&copy; <a href="index.html">Artist Web Site</a> All Rights Reserved.</small>
        <span class="pr"><a href="https://template-party.com/" target="_blank">《Web Design:Template-Party》</a></span>
      </footer>

          <!--ページの上部へ戻るボタン-->
          <div class="pagetop"><a href="#"><i class="fas fa-angle-double-up"></i></a></div>

            <!--開閉ボタン（ハンバーガーアイコン）-->
          <div id="menubar_hdr">
            <span></span><span></span><span></span>
          </div>

            <!--jQueryの読み込み-->
            <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>

            <!--lightbox用jsファイルの読み込み-->
            <script src="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.10.0/js/lightbox-plus-jquery.min.js"></script>

            <!--このテンプレート専用のスクリプト-->
            <script src="js/main.js"></script>
    </footer>
  </body>
</html>